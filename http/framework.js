const http= require("http");

class Rourter{
    constructor(){
        requestAnimationFrame(methos,path,handler)
    }
}
const port=3000;
const server = http.createServer((req,res)=>{
    
res.end(req.url);

})
server.listen(port,()=>console.log(`running port ${port}`));